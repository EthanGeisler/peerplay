# 8.9 — Documentation: Running your own relay

## What was done

Created `docs/relay-guide.md` — a comprehensive guide for running a standalone BoilerDeck relay server.

## Sections included

1. **Introduction** — What a relay is, why run one (federation, own marketplace, data sovereignty)
2. **System Requirements** — CPU, RAM, disk, PostgreSQL, Node.js/Docker
3. **Quick Start with Docker** — Clone, configure, `docker compose up -d`, verify
4. **Manual Setup** — Node.js 22+, PostgreSQL, npm install, prisma migrate, build and start
5. **Environment Variables** — DATABASE_URL, PORT, RELAY_NAME, RELAY_DESCRIPTION, EXTERNAL_RELAYS
6. **API Endpoints** — REST and WebSocket endpoints reference
7. **Nginx Reverse Proxy** — Full config with WebSocket upgrade headers and 24h read timeout
8. **systemd Service** — Unit file, dedicated user creation, enable/start commands
9. **Tor Hidden Service** — torrc config, .onion address retrieval, performance notes
10. **Federation** — Connecting to main relay and others, how it works, discoverability
11. **Monitoring** — Health checks, log commands, WebSocket testing with websocat
12. **Troubleshooting** — Common issues (DB connection, WS timeout, federation, signatures)

## Files changed

- `docs/relay-guide.md` — New file (comprehensive relay guide)
- `docs/handoff/8.9.md` — This handoff document

## Verification

- [x] `docs/relay-guide.md` exists with all required sections
- [x] Includes system requirements, Docker instructions, env config, nginx, systemd, Tor
- [x] Environment variables match actual `.env.example` in relay-server
- [x] Docker compose instructions match actual `docker-compose.yml`
- [x] Dockerfile details (Node 22, Prisma migrate on startup) reflected accurately
