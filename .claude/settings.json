{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'cd /c/Users/eface/peerplay && DIRTY=$(git status --porcelain 2>/dev/null | grep -E \"^( M|\\?\\?|A |M )\" | grep -vE \"settings\\.json$\" | head -5); if [ -n \"$DIRTY\" ]; then echo \"STOP: Uncommitted code changes detected. Complete the post-implementation workflow (review → verify → commit → push → deploy) before stopping.\" >&2; echo \"$DIRTY\" >&2; exit 2; fi'"
          }
        ]
      }
    ]
  }
}
